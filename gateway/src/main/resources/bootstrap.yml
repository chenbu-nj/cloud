spring:
  application:
    name: gateway
  cloud:
    config:
      uri: http://127.0.0.1:8888
      profile: dev
server:
  port: 8060

---
spring:
  cloud:
    config:
      uri: http://127.0.0.1:8888
      profile: dev
  profiles:
    active: devcluster