spring:
  application:
    name: clienta
  cloud:
    config:
      uri: http://127.0.0.1:8888
      profile: dev
server:
  port: 9001

---
spring:
  cloud:
    config:
      uri: http://127.0.0.1:8888
      profile: dev
  profiles:
    active: devcluster1
#此配置必须写在bootstramp.yml才能生效
feign:
  hystrix:
    # 开启断路器
    enabled: true

---
spring:
  cloud:
    config:
      uri: http://127.0.0.1:8888
      profile: dev
  profiles:
    active: devcluster2
#此配置必须写在bootstramp.yml才能生效
feign:
  hystrix:
    # 开启断路器
    enabled: true