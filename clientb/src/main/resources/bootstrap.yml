spring:
  application:
    name: clientb
  cloud:
    config:
      uri: http://127.0.0.1:8888
      profile: dev
  profiles:
    active: dev
#此配置必须写在bootstramp.yml才能生效
feign:
  hystrix:
    # 开启断路器
    enabled: true
---
spring:
  application:
    name: clientb
  cloud:
    config:
      uri: http://127.0.0.1:8888
      profile: dev
  profiles:
    active: devcluster
#此配置必须写在bootstramp.yml才能生效
feign:
  hystrix:
    # 开启断路器
    enabled: true